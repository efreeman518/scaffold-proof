{
  "private": true,
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "^24.13.5"
  },
  "scripts": {
    "test": "npx playwright test",
    "test:full:fast": "npx playwright test --retries=0 --max-failures=4 --timeout=900000",
    "test:blazor": "npx playwright test --project=blazor",
    "test:react": "npx playwright test --project=react",
    "test:uno": "npx playwright test --project=uno",
    "test:uno:release-cold-start": "npx playwright test --project=uno-release-cold-start --retries=0"
  }
}
