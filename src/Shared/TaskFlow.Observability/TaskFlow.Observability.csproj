<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!-- D-053 / package request 30: MessagingTraceContext writes and reads the W3C headers for the broker
         hop, so the app no longer carries its own propagator wiring and OpenTelemetry.Api is not needed
         here - TaskFlowActivitySources is System.Diagnostics only. -->
    <PackageReference Include="EF.Messaging" />
  </ItemGroup>

</Project>
