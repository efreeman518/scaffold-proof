# EF.* 1.1.100 / EF.FlowEngine.* 1.0.169 - slice S0 catalog

Scoping only. No app code changed except `Directory.Packages.props` version pins. Verified 2026-09-10
against the private feed (`efreeman518-github`) and the packages actually restored/decompiled into
`~/.nuget/packages` at the target versions (XML docs where shipped, `ilspycmd 10.1.0.8386` decompile
otherwise). "Landed" = confirmed present in the 1.1.100 / 1.0.169 assembly; "not verified this slice" =
trusted from the package agent's feedback but not independently re-checked here.

## Requests 1-32

| # | Request (short) | App-local fallback site(s) | Package replacement (confirmed) | Migration note | Risk |
|---|---|---|---|---|---|
| 1 | `EntityBase<TId>.Version` (long, replaces `RowVersion`) | `src/Domain/TaskFlow.Domain.Model/Shared/TaskFlowEntityBase.cs:21` | `EF.Domain.EntityBase<TId>.Version` (confirmed: `EF.Domain.xml:125`, `RowVersion` doc still present too, both members exist) | **Behavior differs from expected**: base class now exposes `Version` unconditionally, so `TaskFlowEntityBase<TId>`'s own `Version` property *hides* it (`new` keyword missing) -> CS0108, promoted to a build error by the repo's 0-warnings policy. This is one of the two live compile breaks in this slice. | HIGH - active build error |
| 2 | `DbContextBase<,>.SaveChangesAsync` auto-increments `Version` | `src/Infrastructure/TaskFlow.Infrastructure.Data/Interceptors/VersionTimestampInterceptor.cs:14` | Not independently decompiled this slice (EF.Data.DbContextBase internals); trusted landed per feedback (2 is in the landed list) | Adopting means retiring the custom `SaveChangesInterceptor` in favor of the base class's own version bump - only possible once request 1's `TaskFlowEntityBase.Version` hiding is resolved (they are the same migration). | MEDIUM - blocked on req 1 fix |
| 3 | Split `EF.Data.SqlServer` out of `EF.Data`; move `MigrationSupport`/`RepositoryBase.SetNoLock`/`SetLock` | none (no fallback - open) | **Partial, confirmed**: `EF.Data.SqlServer 1.1.100` now ships `MigrationSupport`, `SqlIdentifier`, `DatabaseFacadeExtensions`, `Interceptors.{ConnectionNoLockInterceptor,ReadUncommittedInterceptor}` (`ilspycmd --list class`). `EF.Data` still references `Microsoft.Data.SqlClient` per feedback (deferred to 2.0). | No app action needed this slice (nothing references `EF.Data.SqlServer` yet). A later slice adding it needs a new `PackageVersion` (feed has `EF.Data.SqlServer 1.1.100` - see New Packages section) and a `PackageReference` on the SQL Server migration project. | LOW - still open by design |
| 4 | `ReadIsolation` enum replacing `bool readNoLock`; gate NOLOCK interceptors on SQL Server | none (no fallback - open) | **Landed, confirmed**: `EF.Data.Contracts.ReadIsolation {Default, ReadUncommitted}` (`EF.Data.Contracts.xml:527-539`); `IRepositoryBase.QueryPage*(..., ReadIsolation readIsolation, ...)` overloads added alongside an `[Obsolete]` `bool readNoLock` overload (doc explicitly notes the old overload's all-optional params force this shape). `EF.Data`'s own `ConnectionNoLockInterceptor`/`ReadUncommittedInterceptor` docs now say "No-op unless the context's provider is SQL Server." | Build is clean of `readNoLock` usage/obsolete warnings today, so the repo isn't calling the old overload - drop-in, no action needed. | LOW |
| 5 | `EF.Data.Encryption` (column encryption + blind index) | 8 markers: `Encryption/{IColumnEncryptor.cs:7,19; BlindIndex.cs:9,22,28; ColumnEncryptionOptions.cs:7,79; ColumnEncryptionOptionsExtension.cs:11,70}` | **Landed, shape matches exactly** per feedback and confirmed via `ilspycmd --list class` on `EF.Data.Encryption 1.1.100`: `BlindIndex`, `BlindIndexInterceptor`, `ColumnEncryptionOptions`, `ColumnEncryptionKeys` (new), `KeyVaultDekProvider`, `ColumnEncryptionOptionsExtension`, `ColumnEncryptionExtensions` (new - likely `AddColumnEncryption`), `AesGcmColumnEncryptor`, `PlaintextColumnEncryptor` (new, dev no-op encryptor). | Drop-in per-type swap; two extra types (`ColumnEncryptionKeys`, `PlaintextColumnEncryptor`) are additive, not blocking. | LOW-MEDIUM - large fallback (8 files) but mechanical |
| 6 | Keyset paging: `KeysetPageAsync`, `CursorPage<T>`, `CursorCodec` | `src/Infrastructure/TaskFlow.Infrastructure.Repositories/TaskItemRepositoryQuery.cs:56`, `TaskViewKeysetToken.cs` | **Landed, confirmed, and already colliding**: `EF.Common.Contracts.CursorPage<T>`/`CursorSearchRequest<TFilter,TSortMode>`/`PageSizeLimits` (request 21) plus `EF.Data.Contracts.{CursorCodec, KeysetCursor, KeysetSortKey}` (tenant-bound, schema-versioned - request 27). No generic `KeysetPageAsync<T,TKey>` LINQ helper found; the codec/cursor types are the shipped shape instead. | **This is the other live compile break**: `TaskFlow.Application.Models.Paging.CursorPage<T>` (app-local, kept "app-local this release" per the original request text) now collides with `EF.Common.Contracts.CursorPage<T>` -> CS0104 ambiguous reference in `TaskFlowJsonContext.cs:87`, cascading into a SYSLIB1030 source-gen failure. Full redesign: retire the app's own `Paging.{CursorPage,CursorSearchRequest,PageSizeLimits,TaskItemCursorSearchRequest}` (4 files) and `TaskViewKeysetToken.cs`/`DataProtectionCursorProtector.cs` in favor of the package types - touches the ~46-file fan-out found by grepping `CursorPage\|CursorSearchRequest\|PageSizeLimits`. | HIGH - active build error, largest structural finding |
| 7 | `IRepositoryBase.UpsertAsync` via FlexLabs | `TaskItemSystemRepository.cs:83`, `InboxStore.cs:12`, `RelationalTaskViewRepository.cs:41` | **Landed, confirmed**: `EF.Data.RepositoryBase<,,>.UpsertAsync<T>(entity)` (parameterless, unchanged) plus new `UpsertAsync<T>(entity, match, update, ct)` and `UpsertRangeAsync<T>(...)` (FlexLabs `UpsertCommandBuilder` under the hood - `EF.Data.xml:786-800`). | Additive - existing parameterless calls keep working; direct FlexLabs call sites can switch to the new overload. | LOW |
| 8 | `PostgreSqlContainerFixture`, `DbContextOptionsFactory.BuildNpgsqlOptions<T>` | `tests/Test.Support/Hosting/TestDatabaseContainer.cs:24` | **Landed, confirmed**: `EF.IntegrationTesting.Testcontainers.PostgreSqlContainerFixture` (`StartAsync`/`DisposeAsync`, default image via `DefaultImage`) (`EF.IntegrationTesting.xml:210-238`). | Mechanical fixture swap. | LOW |
| 9 | Drop hardcoded `nvarchar(max)` from FlowEngine SQL store rows | `src/Infrastructure/TaskFlow.Infrastructure.Data/TaskFlowFlowEngineDbContext.cs:54-58` | **Landed, confirmed**: decompiled `EF.FlowEngine.Outbox.Sql 1.0.169`'s `FlowEngineOutboxRow.Payload` carries no `[Column(TypeName=...)]` attribute (was present before). | The app's `.HasColumnType(null)` shim in `OnModelCreating` is now a no-op; delete it and the `// fallback:` comment (2 lines). | LOW - dead code removal |
| 10 | `ExecuteUpdateBatchedAsync`/`ExecuteDeleteBatchedAsync` | `src/Infrastructure/TaskFlow.Infrastructure.Repositories/BatchedExecute.cs:6` | **Landed, confirmed**: `EF.Data.Contracts.BatchedExecute.{RunBatchedAsync, ExecuteDeleteBatchedAsync (2 overloads), ExecuteUpdateBatchedAsync, KeyIn}` (`EF.Data.Contracts.xml:33-102`). | Signature differs: package takes `IQueryable<T>` + filter expression + `UpdateSettersBuilder<T>` action, batchSize/maxBatches - needs an adapter over the app's own loop shape. | LOW-MEDIUM |
| 11 | `AddLeasedWorkerService<TWorker,TOptions>()` / `LeasedWorkerBase.ProcessBatchAsync` | `src/Host/TaskFlow.Scheduler/Workers/LeasedWorkerBase.cs:8` | **Landed, confirmed**: `EF.BackgroundServices.Leased.LeasedWorkerBase<TOptions>` (`ProcessBatchAsync`, `ExecuteAsync`, `NextDelay`, `ApplyJitter`, `NewLeaseToken`), `LeasedWorkerOptions`, `ServiceCollectionExtensions.AddLeasedWorkerService<,>` (`EF.BackgroundServices.xml:134-320`). | Base-class swap for `OutboxDispatcherService`/`BlobDeleteWorkerService`; must verify `PollInterval`/`IdleBackoffMax`/`LeaseDuration`/`BatchSize`/`MaxAttempts`/`JitterFactor` semantics match the app's current backoff behavior exactly before cutover. | MEDIUM |
| 12 | `DeterministicGuid.Create(ns, parts)` (UUIDv5) | `src/Domain/TaskFlow.Domain.Shared/DeterministicGuid.cs:6` | Not independently decompiled this slice; trusted landed (12 is in the landed list, EF.Common owns it) | Small (~20 line) drop-in removal once confirmed. | LOW |
| 13 | `EF.Cache.ITypedCache` (tagging/profiles/degraded events) | `FusionTaskFlowCache.cs`, `CacheSettings.cs` (`Infrastructure.Caching`) | **Partially confirmed**: `EF.Cache.TypedCache` type exists at 1.1.100 (seen via xml grep); interface shape (tagging/profiles/degraded events) not individually verified. App does **not** reference `EF.Cache` in any csproj today - it hand-rolls FusionCache registration directly. | Nice-to-have; adopting means taking a new dependency on `EF.Cache`, not a signature-level swap. | MEDIUM (bundled with req 31) |
| 14 | `ServiceBusSenderPool`, `IntegrationEventEnvelope`, `EnvelopeSerializer` | `Application.Contracts/Messaging/IntegrationEventEnvelope.cs`, `ServiceBusEventTransport.cs` sender cache | Not decompiled this slice - verify `EF.Messaging` doesn't ship its own `IntegrationEventEnvelope` type before adopting (same collision pattern as req 6/21). | Check for a namespace collision first. | MEDIUM (unverified) |
| 15 | `EF.Table.DeleteByPartitionRangeAsync(prefix, cutoff, batchSize)` | Azure Table audit retention (no single fallback file) | **Not found**: `EF.Table 1.1.100`'s public surface is still only `ITableEntity, ITableRepository, TableRepositoryBase, TableRepositorySettingsBase` (`ilspycmd --list class`) - no `DeleteByPartitionRangeAsync` member seen. | Re-verify before relying on it; may not have landed despite being in the "landed" list (or it's a member on `ITableRepository` not surfaced by the class listing - needs an interface-level check). | MEDIUM - unconfirmed |
| 16 | `EfWebApplicationFactoryBase.BuildOptionsFor<TContext>` virtual | none named | **Landed, confirmed**: `EF.IntegrationTesting.AspNetCore.EfWebApplicationFactoryBase<,,>.BuildOptionsFor<TContext>(connectionString)` is virtual, defaults to SQL Server with retry-on-failure, documented as the override point for Npgsql (`EF.IntegrationTesting.xml:78-85`). | Drop-in. | LOW |
| 17 | Split `Clients.Sql.AdHocSqlQueryClient` into `Clients.SqlServer`; README | none (app has zero references to `AdHocSqlQueryClient` today) | **Landed, confirmed**: new package `EF.FlowEngine.Clients.SqlServer 1.0.169` ships `AdHocSqlQueryClient`, `SqlServerQueryClientExtensions` (`ilspycmd --list class`). README already done per feedback. | Zero impact today (unused). A future slice adding ad hoc SQL query nodes needs a new `PackageVersion`/`PackageReference` (feed confirmed at 1.0.169 - see New Packages). | LOW - unused |
| 18 | `IntegrationNodeConfig.Headers` | `src/Host/TaskFlow.Bootstrapper/Registration/FlowEngineIfMatchOverrideHandler.cs` (+ `RegisterServices.FlowEngine.cs`, `LogMessages.cs`) | **BREAKING, landed, confirmed**: `EF.FlowEngine.Definition.NodeConfigs.IntegrationNodeConfig.Headers` now documented (`EF.FlowEngine.xml:2309`). See Breaking Items below. | Remove `FlowEngineIfMatchOverrideHandler` + its registration + `tests/Test.Unit/FlowEngineIfMatchOverrideHandlerTests.cs`; delete the now-failing-by-design `IntegrationNodeConfig_HasNoHeadersProperty_PackageRequest18` guard test. No workflow JSON change - `ai-task-triage.json`'s PATCH nodes already carry `"headers": {"If-Match":"*"}`. | HIGH - active test failure by design |
| 19 | Per-iteration idempotent id for loop-created entities | none (workflow-config gap, not code) | **Landed differently than requested, confirmed**: no `newGuidV7()`; instead `LoopNodeConfig.IdAs` (default `"currentItemId"`, a deterministic RFC4122 v5 UUID of instance id + node id + iteration index) (`EF.FlowEngine.xml:2342-2352`). | `ai-task-decomposer.json`'s `n-loop-create-one` POST body (lines 82-90) sets `ParentTaskItemId/TenantId/Title/EstimatedEffort` but no `Id` - needs `"Id": "{{$.context.currentItemId}}"` added to make the create idempotent on retry. Depends on assumption A2 (`ValueGeneratedNever`, already true). | MEDIUM - workflow JSON change needed |
| 20 | `ProblemDetailsHelper` (412/428/409) + `IfMatchEndpointFilter`/`ETagEndpointFilter` | `src/Host/TaskFlow.Api/Filters/{IfMatchEndpointFilter.cs, ETagEndpointFilter.cs, IfMatch.cs}` | **Landed, confirmed**: `EF.AspNetCore.Filters.{IfMatchEndpointFilter<T>, ETagEndpointFilter<T>}` and `EF.AspNetCore.ProblemDetailsHelper.{DefaultStatusCodeMapping, BuildProblemDetailsResponse, BuildProblemDetailsResponseMultiple}` with a custom status-mapping resolver delegate (`EF.AspNetCore.xml:121-263`). | Shape matches; fan-out is wide - 17 files reference the app's own filters today (`TaskItemEndpoints.cs` + all 8 `*CqrsEndpoints.cs` + `CategoryEndpoints.cs`, `ChecklistItemEndpoints.cs`, `CommentEndpoints.cs`, `AttachmentEndpoints.cs`, `TagEndpoints.cs`, plus the Bootstrapper handler). | MEDIUM - mechanical but wide review surface |
| 21 | Promote `CursorPage`/`CursorSearchRequest`/`PageSizeLimits` to `EF.Common.Contracts` | `Application.Models/Paging/{CursorPage.cs, CursorSearchRequest.cs, PageSizeLimits.cs, TaskItemCursorSearchRequest.cs}` | **Landed, confirmed, causing the CS0104 build break** - see request 6 (same finding). | See request 6. | HIGH - active build error, same as req 6 |
| 22 | `WherePropertyIn` over a navigation collection | none named | **Landed, confirmed**: `EF.Data.Contracts.IQueryableExtensions.WherePropertyIn` has both the flat overload (``2) and a navigation-collection `Any(Contains)` overload (``3, `Expression<Func<T,IEnumerable<T1>>>` + `Expression<Func<T1,T2>>`) (`EF.Data.Contracts.xml:222-235`). | Drop-in, additive. | LOW |
| 23 | New `EF.Messaging.RabbitMq` (connection multiplexer, topology, consumer host) | `src/Packages/EF.Messaging.RabbitMq` (already built, TaskFlow-free) + `tests/EF.Messaging.RabbitMq.Tests` | **Landed, confirmed**: feed package `EF.Messaging.RabbitMq 1.1.100` ships the identical public surface (`RabbitMqConnectionMultiplexer`, `RabbitMqConsumerHostedService`, `RabbitMqTopology*`, `RabbitMqPublisher`, `RabbitMqOptions*`, `RabbitMqServiceCollectionExtensions`, `RabbitMqHealthCheck`, `RabbitMqMetrics` - `ilspycmd --list class`). Not yet referenced by any csproj (pinned but unused, per instructions). | Per feedback: move the two projects out of this repo, then swap `TaskFlow.Infrastructure.Messaging.RabbitMq`'s `ProjectReference` to a `PackageReference` on `EF.Messaging.RabbitMq 1.1.100`. Not a rewrite. | MEDIUM - mechanical, project deletion + reference swap |
| 24 | `RabbitMqHeaders.AsString` made public | consumers re-implement UTF-8 header decoding individually | **Landed, confirmed**: `EF.Messaging.RabbitMq.RabbitMqHeaders` is a public top-level type in the feed package. | Bundled with request 23's migration - once adopted, the app's local re-implementations in `RabbitMqConsumerHandlers.cs`/`RabbitMqEventTransport.cs` become redundant. | LOW - bundled with 23 |
| 25 | S3/`IAmazonS3` backend behind a provider-neutral storage interface | `src/Infrastructure/TaskFlow.Infrastructure.Storage/S3/S3ObjectStorageRepository.cs` | **Landed, confirmed, new package**: `EF.Storage.Contracts 1.1.100` ships `IObjectStorageRepository` (`UploadAsync, DownloadAsync, DeleteAsync, ExistsAsync, GetPresignedUrlAsync, ListAsync`) + `ObjectStorageItem`/`ObjectStoragePage`, vendor-SDK-free (`EF.Storage.Contracts.xml:7-118`). `EF.Storage.BlobRepositoryBase` now implements both `IBlobRepository` and this new `IObjectStorageRepository`. | Real redesign, not a rename: the app's own `IBlobStorageRepository` (Application.Contracts) needs reconciling with `IObjectStorageRepository` so both the Azure and S3 arms implement one shared interface (D-037). | HIGH - shared abstraction change across both storage lanes |
| 26 | Relational audit backend / audit-specific contract | `src/Infrastructure/TaskFlow.Infrastructure.Repositories/RelationalAuditLogRepository.cs` | **Landed, confirmed, new package**: `EF.Audit.Contracts 1.1.100` ships `IAuditLogRepository`, `AuditRecord`, `AuditLogQuery`, `AuditLogPage`, `AuditSettings`, `AuditLogSettingsBase` (`ilspycmd --list class` + xml). Resolved via a contracts-only package (the acceptance criteria's second option), not a relational `ITableRepository` impl. | Behavior differs: need to map the app's `AuditLogRecord` to `AuditRecord`, and ideally have both the Azure Table arm (`AuditLogRepository.cs`) and relational arm implement the same `IAuditLogRepository`. Intersects directly with assumption A1 (Azure Table RowKey non-idempotency) - fix that before/while adopting. | HIGH - touches both audit backends |
| 27 | Tenant-bound, schema-versioned keyset continuation token | `RelationalTaskViewRepository.cs` (`v1|tenant|ticks|id` Base64Url token), `TaskViewKeysetToken.cs` | **Landed, confirmed** - see request 6: `EF.Data.Contracts.CursorCodec` carries a tenant key (re-checked on decode) and a schema-version byte in the payload, exactly as requested. | Folded into the request 6/21 redesign - not a separate migration. | Bundled with 6/21 |
| 28 | OpenAI-compatible chat/embedding client factory | `src/Host/TaskFlow.Bootstrapper/Registration/{OpenAICompatibleChatClientFactory.cs, RegisterServices.AiChatClient.cs}` | **Landed in EF.AI instead of EF.Common, confirmed**: `EF.AI.Chat.ServiceCollectionExtensions.{AddEFChatClient (3 overloads), AddEFChatClientFromAspire, ApplyAspireConnection}`, `EFChatClientSettings`, `EFChatClientProvider` enum, mirrored by `EF.AI.Embeddings.EmbeddingServiceCollectionExtensions`/`EFEmbeddingGeneratorProvider` (`EF.AI.xml:7-372`). Provider enum gained `OpenRouter`/`Ollama`/`vLLM` per feedback (not independently enumerated this slice). | Not a drop-in method swap: package uses a DI-registration-extension pattern (`AddEFChatClient` on `IServiceCollection`), the app uses a factory class. Delete `OpenAICompatibleChatClientFactory.cs`, rewire `RegisterServices.AiChatClient.cs` to call `AddEFChatClient`/`AddEFEmbeddingGenerator`, and map any app-specific provider enum onto `EFChatClientProvider`. | HIGH - registration-pattern change, not signature-level |
| 29 | gRPC error-status mapping, `IncludeLogDataInResponse` default, pluggable `HttpMessageHandler` | Not used - `TaskFlowReadGrpcService.cs` bypasses `EF.Grpc` entirely, doing its own `RpcException` mapping | **Landed, confirmed**: `EF.Grpc.ErrorInterceptorSettings.StatusCodeMapper` (`Func<Exception,StatusCode>`, defaults to `Internal`) and `.IncludeLogDataInResponse` (doc: "Defaults to false since the trailer is wire-visible to every caller" - flipped from the old default-true). `AddGrpcClient2` now takes `HttpMessageHandler primaryHttpMessageHandler` directly. | Zero impact today (unused). Informational for a future slice that wants to adopt `EF.Grpc` for `TaskFlowReadGrpcService`. | LOW - unused |
| 30 | W3C trace-context inject/extract for messaging | `src/Shared/TaskFlow.Observability/Tracing/MessagingTrace.cs` (G2/D-053) | **Landed, confirmed, exact shape requested**: `EF.Messaging.Tracing.MessagingTraceContext.{Inject(Activity, Action<string,string>), Inject(Activity, IDictionary<string,object>), Extract(Func<string,string>), Extract(IDictionary<string,object>), FormatTraceParent, AsString}` (`EF.Messaging.xml:453-520`). | Signature close but not identical to the app's own `MessagingTrace` (delegate-based `Action<string,string>` inject vs. whatever shape the app chose) - mechanical adapter across the 8 call sites found (`RabbitMqEventTransport.cs`, `RabbitMqConsumerHandlers.cs`, `ServiceBusEventTransport.cs`, `RecurringTaskGenerationHandler.cs`, `OverdueTaskCheckHandler.cs`, `ServiceBusEnvelopeReader.cs`, `IntegrationEventConsumers.cs`, `IntegrationEnvelopeReader.cs`). | MEDIUM - wide fan-out, mechanical |
| 31 | `CacheSettings.Serializer` (Json\|MessagePack) + schema-version key prefix | `src/Infrastructure/TaskFlow.Infrastructure.Caching/CacheSettings.cs` | **Landed, confirmed**: `EF.Cache.CacheSettings.Serializer` + `.SchemaVersion` (doc explicitly cross-references retiring cached shapes by key), `EF.Cache.CacheSerializer {Json, MessagePack}`; much richer settings surface than before (Redis config section, backplane, failsafe/jitter timeouts). App does **not** reference `EF.Cache` today (`RegisterCachingServices.cs` wires FusionCache directly). | Not a field rename - adopting means replacing the app's own FusionCache registration with `EF.Cache.CacheServiceCollectionExtensions`, a much larger change than the request implies. | HIGH - registration-pattern change |
| 32 | `IDistributedLock` distributed mutual exclusion | `Application.Contracts/Locking/IDistributedLock.cs`, `Infrastructure.Caching/Locking/{RedisDistributedLock.cs, InProcessDistributedLock.cs}` | **Landed, confirmed, split across two packages**: `EF.Common.InProcessDistributedLock` (`TryAcquireAsync(key, ttl, ct) -> IAsyncDisposable?`, doc explicitly says "register `EF.Cache.RedisDistributedLock` instead when a Redis connection exists") + `EF.Cache.RedisDistributedLock` (`SET NX PX` + Lua compare-and-delete release, 3 ctor overloads incl. an externally-owned `IConnectionMultiplexer`). | Shape matches the acceptance criteria exactly. Adopting the Redis half means taking the `EF.Cache` dependency the app has avoided so far (bundled with req 13/31). | MEDIUM |

## Breaking items (called out separately in the feedback)

| Item | Files affected | Finding |
|---|---|---|
| Req 18 - `IntegrationNodeConfig.Headers` added | `tests/Test.Integration.FlowEngine/WorkflowDefinitionValidityTests.cs:147` (`IntegrationNodeConfig_HasNoHeadersProperty_PackageRequest18`); `src/Host/TaskFlow.Bootstrapper/Registration/FlowEngineIfMatchOverrideHandler.cs` + `RegisterServices.FlowEngine.cs` + `LogMessages.cs`; `tests/Test.Unit/FlowEngineIfMatchOverrideHandlerTests.cs` | Confirmed: `typeof(IntegrationNodeConfig).GetProperty("Headers")` now returns non-null, so `Assert.IsNull(...)` fails by design - this is the expected build/test signal that the mitigation can be removed. No workflow JSON change needed. |
| `AdHocSqlQueryClient` moved to `EF.FlowEngine.Clients.SqlServer` | none - zero references to `AdHocSqlQueryClient` anywhere in `src`/`tests` | Confirmed via decompile the type now lives in the new package. Zero impact on this repo today. |
| gRPC `grpc-message` trailer no longer carries exception text by default | none - `TaskFlowReadGrpcService.cs` does not use `EF.Grpc` | Confirmed `IncludeLogDataInResponse` default flipped to `false` in `EF.Grpc.ErrorInterceptorSettings`. Zero impact today (package unused). |
| `EventHubProducerBase.SendBatchAsync` now throws after a failed publish | none - no Event Hub usage anywhere in `src` (grep for `EventHub` found zero files) | Zero impact today; purely informational for a future Event Hub transport. |

## Assumptions flagged by the package agent

| Item | Files affected | Finding |
|---|---|---|
| Azure Table audit arm is not replay-idempotent | `src/Infrastructure/TaskFlow.Infrastructure.Storage/AuditLogRepository.cs:40-58`, `AuditLogTableEntity.cs` | Confirmed: `recordedUtc = DateTimeOffset.UtcNow` is computed fresh on every write and folded directly into `RowKey` (`rowKey = $"{DateTime.MaxValue.Ticks - recordedUtc...}_{entry.Id:N}"`). A redelivered/replayed audit write at a different wall-clock time produces a different RowKey, i.e. a duplicate row rather than an idempotent overwrite. Needs a stable-per-event RowKey (e.g. derived from the audited event's own id/version, not write time) before or while adopting `EF.Audit.Contracts` (request 26). |
| Native upsert requires client-generated keys to be `ValueGeneratedNever` | `src/Infrastructure/TaskFlow.Infrastructure.Data/Configurations/{EntityBaseConfiguration.cs:20, OperationalConfiguration.cs:17, AuditLogConfiguration.cs:19}` | Confirmed: all three configurations already call `builder.Property(e => e.Id).ValueGeneratedNever()`. No action needed - the assumption already holds for this repo, including for request 19's new client-generated UUIDv5 loop ids. |

## New packages confirmed in the feed at the target version (not yet referenced by any csproj)

All confirmed present via `dotnet package search --exact-match` against `efreeman518-github`. None were added to `Directory.Packages.props` this slice (out of scope - "do not reference them from any csproj yet").

| Package | Version confirmed | Purpose (from this catalog) |
|---|---|---|
| `EF.Data.SqlServer` | 1.1.100 | Request 3 - SQL-Server-only migration/lock helpers split out of `EF.Data` |
| `EF.Data.Encryption` | 1.1.100 | Request 5 - column encryption / blind index |
| `EF.Storage.Contracts` | 1.1.100 | Request 25 - provider-neutral `IObjectStorageRepository` |
| `EF.Audit.Contracts` | 1.1.100 | Request 26 - audit-specific repository contract |
| `EF.Messaging.RabbitMq` | 1.1.100 | Requests 23/24 - RabbitMQ transport (the app's own `src/Packages/EF.Messaging.RabbitMq` is the pre-publish twin of this) |
| `EF.FlowEngine.Clients.SqlServer` | 1.0.169 | Request 17 - `AdHocSqlQueryClient`, unused today |

Note: the guessed id `EF.Audit.Data` does not exist in the feed - the real package is `EF.Audit.Contracts`.

## Restore / build result

Restore: `dotnet restore TaskFlow.slnx -p:Configuration=Release` - **0 errors, 0 warnings, 50 projects**.

Build: `dotnet build TaskFlow.slnx --no-restore --configuration Release -m:1` - **FAILED, 3 distinct compile errors** (each reported twice by MSBuild = "6 Error(s)" in the summary; 0 warnings otherwise, no `TaskFlow.Functions` obj-tree noise):

1. `src/Application/TaskFlow.Application.Models/Serialization/TaskFlowJsonContext.cs(87,26)`: **CS0104** - `'CursorPage<>' is an ambiguous reference between 'TaskFlow.Application.Models.Paging.CursorPage<T>' and 'EF.Common.Contracts.CursorPage<T>'` (request 6/21 landing).
2. Same file/line: **SYSLIB1030** - "Did not generate serialization metadata for type `CursorPage<TaskItemDto>`" - downstream of error 1 (the source generator cannot resolve the ambiguous type).
3. `src/Domain/TaskFlow.Domain.Model/Shared/TaskFlowEntityBase.cs(27,17)`: **CS0108** - `'TaskFlowEntityBase<TId>.Version' hides inherited member 'EntityBase<TId>.Version'` (request 1 landing), promoted from warning to build error by the repo's 0-warnings policy.

Both are exactly the two compile breaks anticipated by requests 1 and 6/21 landing as designed. Fixing them is explicitly out of scope for this slice (later slices).
