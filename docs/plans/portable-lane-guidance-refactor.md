# Portable Lane + Guidance Alignment Refactor - Orchestration Handoff

Durable state for the orchestrated refactor. Sessions and agents are disposable; this doc plus `docs/plans/portable-lane-guidance-plan.md` (the approved plan) must be enough to resume.

## Goal

Add the Portable hosting lane (Docker Compose + Caddy on a VPS, external PostgreSQL and LLM provider, Azure only for Key Vault + App Configuration) as independent provider switches with a lane preset, and close every gap against the scale-guidance checklist (runtime profile, source-generated JSON, hot paths, bounded concurrency, health probe contract, edge hardening, hedging, distributed lock, broker trace propagation, internal gRPC, MessagePack L2, feature flags, pgvector search). Plan sections: Part A (lane), Part B (alignment), decisions D-035..D-055.

## Decisions (confirmed by the user 2026-09-08)

- Keep the own transport (`IIntegrationEventTransport`, `EF.Messaging.RabbitMq`); no MassTransit or SlimMessageBus (D-046).
- Portable compute = Docker Compose + Caddy on one VPS, hand-written under `deploy/compose/`; no `Aspire.Hosting.Docker` (D-036).
- Build now: pgvector semantic search (P7), gRPC internal read proof (G3), MessagePack L2 serializer (G3).
- Delivery: slices merge into `feature/scale-baseline-dual-provider` in the primary checkout; one integration commit per green slice; no push, no PR until the user asks.
- Health paths fixed for every slice: `/healthz/live`, `/healthz/ready`, `/healthz` aggregate (D-049). gRPC h2c port for the Api: 8081 (D-054).
- Environment: Testcontainers lanes need a run-scoped `TESTCONTAINERS_HOST_OVERRIDE=<podman machine ip>` (never committed); Test.Aspire and compose smoke cannot run on this machine (Podman WSL port binding) - CI is the proof.

## Slice table

Worktrees live in `C:\Users\EbenFreeman\source\repos\scaffold-proof-wt\<slice>` unless the harness chose its own path. Base for every slice branch: `feature/scale-baseline-dual-provider` at spawn time.

| id | scope (plan sections) | model | status | branch | worktree | agent id |
|---|---|---|---|---|---|---|
| P1 | Artifacts: D-035..D-055, resource-implementation.yaml, UBIQUITOUS-LANGUAGE, domain-specification, INSTRUCTION-GAPS | sonnet | done (merged) | feature/pl-p1-artifacts | removed | a2c5c8672f43fea50 |
| G1 | Runtime props + Dockerfiles, TaskFlowJsonContext, export writer, pooled publish buffers, bounded blob deletes + dispatcher fan-out, blocking-call sweep, package AOT flags, benchmark (B.2 G1) | opus | done (merged) | feature/pl-g1-runtime | removed | a57532ee14de929e6 |
| P2 | Six switch skeletons with existing arms + fallbacks, Data Protection switch, lifted credential, pooler option, selector tests, architecture rules (A.1) | sonnet | done (merged) | feature/pl-p2-switches | removed | a8d6f9235007166e8 |
| G2 | Health contract + Bicep probes/sticky, YARP hardening + edge limiter, hedging, ActivitySources + trace propagation, IDistributedLock (B.2 G2) | opus | done (merged) | feature/pl-g2-edge-health-tracing | removed | a5d39f4fa0c943b96 |
| P3 | Relational read model + relational audit: entities, configs, migrations both providers, repositories, arms, parity tests (A.1) | opus | done (merged 229d8a5) | feature/pl-p3-relational-readmodel-audit | removed | abd00550c3d0c5c25 |
| P4 | S3 object storage + MinIO fixture + health + provisioning + endpoint tests (A.1) | sonnet | done (merged) | feature/pl-p4-s3 | removed | ac78f634d57d41d78 |
| P5 | App Configuration + FeatureManagement + tenant targeting + flags + OpenAI-compatible arm (A.1) | sonnet | done (merged) | feature/pl-p5-appconfig-flags-ai | removed | aaee39f05c7d3866d |
| G3 | gRPC read service + Blazor client + ports; MessagePack L2 switch (B.2 G3) | opus | done (merged) | feature/pl-g3-grpc-messagepack | removed | ac27fff1148042c4f |
| P6 | Compose lane, Aspire lane preset + MinIO, reusable image workflow + deploy-vps.yml, ci compose config/smoke, Bicep pgbouncer, Test.Aspire portable topology (A.2) | opus | done (merged) | feature/pl-p6-compose-lane | removed | a68f7223cae1d3253 |
| P7 | PgVector semantic search: entity + migration, embedding consumer + topology, search service, fail-fast, tests (A.1) | opus | done (merged) | feature/pl-p7-pgvector | removed | a515285d389e4b2e8 |
| G4 | LoggerMessage sweep + CA1848, alignment doc, tech-design, README, REFERENCE-STATUS, scaffold-instructions-handoff, ef-package-requests, language (B.2 G4) | sonnet | done (merged) | feature/pl-g4-docs | removed | af81dc5450984503e |
| F1 | Follow-up (user 2026-09-09): Functions App Configuration wiring + OpenAI deployed dependency, Api launchSettings aligned to Kestrel:Endpoints, pgvector watch-item wording (0.3.0 is the newest release), compose/AppHost image tags pinned (otel-lgtm 0.32.1, minio RELEASE.2025-09-07T16-13-09Z, pgbouncer v1.25.2-p0) | sonnet | running | feature/pl-f1-loose-ends | scaffold-proof-wt/pl-f1 | ada657dc3f15c9f44 |

Waves: 1 = P1 + G1; 2 = P2 + G2 (after wave 1 merged); 3 = P3 + P4 + P5 + G3 (after wave 2 merged); 4 = P6 + P7 (after wave 3 merged); 5 = G4 after full acceptance.

## Merge gate per slice

Diff reviewed by the orchestrator in the agent's worktree; `dotnet build TaskFlow.slnx` and the Uno build at 0 warnings; `dotnet test TaskFlow.slnx --filter "TestCategory=Unit|TestCategory=Architecture|TestCategory=Endpoint"` green; Testcontainers lanes the slice touched on both `TASKFLOW_TEST_DB_PROVIDER` values; `az bicep build` when infra changed; `docker compose config -q` when compose changed; vulnerability audit clean. Then `git merge --no-ff feature/pl-<slice>` into the integration branch in the primary checkout.

## Session log

- 2026-09-08: inventories (Azure surfaces, runtime alignment, infra assets) and the portable-lane design completed; the guidance-alignment design agent was stopped by a user interrupt, so the alignment slicing was done by the orchestrator from the inventory evidence. User decisions recorded above. Plan and handoff committed. Next action: spawn wave 1 (P1 with harness worktree, G1 with in-prompt worktree).
- 2026-09-08: P1 merged (artifacts). Note: new entities TaskView/AuditLog/TaskItemEmbedding sit in the entities list tagged with dataStore readModel/audit/postgresOnly; the embedding path is a fourth subscription on DomainEvents bound to TaskItemCreatedEvent, TaskItemStatusChangedEvent, TaskItemRescheduledEvent, TaskItemCompletedEvent (no generic TaskItemUpdatedEvent exists). aiServices.search.provider is now a list spelled AzureAiSearch. G1 running.
- 2026-09-08: G1 merged (7 commits, 38 files). Fast lane now 498 (Unit|Architecture|Endpoint). Shapes downstream slices must use: TaskFlowJsonContext (Application.Models), TaskFlowMessagingJsonContext (Application.Contracts, envelope + event payloads; IntegrationEventEnvelope.From THROWS for an unregistered event record - register new events there), TaskFlowApiJsonContext (Api, ProblemDetails); src/Host/TaskFlow.Host.props imported by the six hosts; Blazor and Functions Dockerfiles use aspnet:10.0-noble-chiseled-extra (ICU) with an architecture test pinning the pairing; EnableRequestDelegateGenerator on in the Api; EF.Messaging.RabbitMq IsAotCompatible; Api/Gateway Dockerfiles publish ReadyToRun (-r linux-x64 on restore too). OPEN ITEM for P5: the Api publish output contains Microsoft.Agents.AI.OpenAI.dll but NOT OpenAI.dll (OpenAI is PackageReference PrivateAssets=all in Api and Bootstrapper, hence PublishReadyToRunExclude in TaskFlow.Api.csproj). The OpenAICompatible arm needs OpenAI.dll deployed: P5 must make OpenAI a deployed dependency (resolve whatever version pin motivated PrivateAssets=all) and remove the R2R exclude. Benchmark: export 1,000 rows 192,072 B -> 136 B allocated. P2 running; G2 spawned.
- 2026-09-08: P2 merged (33 files). Fast lane 561. Shapes: HostingLane/HostingLaneSelector in Application.Contracts/Configuration (env TASKFLOW_LANE > Hosting:Lane > Azure); Bootstrapper Registration/HostingLane.cs holds LaneDefaults.Portable; selectors RegisterServices.{Storage,ReadModel,Audit,DataProtection}.cs + Search in Infrastructure.AI/ServiceCollectionExtensions.cs + AI in RegisterServices.AiChatClient.cs, each env > config > lane default > hard default, unknown value throws; unimplemented arms throw NotSupportedException naming the slice (S3 -> P4, Relational -> P3, PgVector -> P7, OpenAICompatible -> P5); [ProviderSwitch(typeof(contract))] attribute on dispatchers drives ProviderSwitchArchitectureTests; NoOpBlobStorageRepository registered when AzureBlob has no connection string; Data Protection lifted to RegisterServices.DataProtection.cs (Redis arm eager ConnectionMultiplexer.Connect, documented shortcut); PoolerMode in TaskFlowDbProvider.cs; AGENTS.md documents the new env vars. System.Security.Cryptography.Xml bumped to 10.0.12 (NU1605). G2 running; spawning P3, P4, P5.
- 2026-09-08: P4 merged (16 files). Fast lane 564. Shapes: Storage:S3:{ServiceUrl, PublicServiceUrl (required, fail-fast), Region, AccessKeyId, SecretAccessKey, ForcePathStyle, DownloadUrlLifetime}; S3ObjectStorageRepository + keyed IAmazonS3 "s3-public" client for presigned URLs (SigV4 signs Host, so no host rewriting; Protocol derived from the URL scheme); IS3BucketProvisioner keeps Amazon.* out of Bootstrapper; MinioContainerFixture in Test.Integration. P6 must supply Storage__S3__* to hosts (AppHost MinIO + compose). G2, P3, P5 running.
- 2026-09-08: P3 merged (20 files; one trivial conflict in ProviderSwitchSelectorTests.cs resolved by keeping P4's S3-resolves test). Both DB lanes: Test.Integration 50/50 each; has-pending-model-changes clean; fast lane 573 in P3's tree. Shapes: TaskViewRecord (+TaskViewBody JSON context) and AuditLogRecord on TaskFlowDbContextBase (both contexts); RelationalTaskViewRepository reads via Query, writes via Trxn; keyset token v1|tenant|ticks|id Base64Url, tenant re-checked, invalid -> ArgumentException -> 400; counter patch = one ExecuteUpdateAsync with a statement-bodied setter; audit append = FlexLabs insert-only upsert; Relational audit arm also binds AuditLogStorageSettings. Test.Endpoints uses UseInMemoryDatabase (no ExecuteUpdate/Upsert) so relational-arm endpoint coverage lives in Test.Integration. Note: Postgres timestamptz is microsecond precision vs SQL Server 100ns. G4 counts: fast lane 573+, Test.Integration 50 per lane. G2, P5 running.
- 2026-09-08: integration gate on 9f7cbd6 (P1, G1, P2, P4, P3 merged): build 0 warnings, fast lane 576 passed. Waiting on G2 and P5 (both told to merge the integration branch before their gates); G3 spawns after G2 merges; P6 and P7 after wave 3 completes.
- 2026-09-08: G2 merged (42 files). Fast lane 583 in G2's tree. Shapes: /healthz/live (live: self), /healthz/ready (ready: sql, scheduler, outbox, rabbitmq), /healthz aggregate; /readyz removed; container-app.bicep liveness + startup probes, stickySessions param (Blazor only), Api/Gateway readinessPath overrides dropped (Api's pointed at the authorized /health/db - latent bug); infra/main.json regenerated. Gateway: RateLimiting:Edge (EdgeRateLimitSettings; per-IP token bucket 200/s chained with a 1000 concurrency limiter; probes exempt); YARP active/passive health, PowerOfTwoChoices, 30s activity timeout, HTTP/2. ServiceDefaults ReadHedgingExtensions (GET-only via ShouldHandle AND DelayGenerator infinite for non-GET) applied to the Blazor read client; Cosmos:Hedging:Enabled -> CrossRegionHedgingStrategy (Cosmos 3.62.1). Observability TaskFlowActivitySources + MessagingTrace (producer spans in the transports, consumer = parent). IDistributedLock: RedisDistributedLock / InProcessDistributedLock chosen in RegisterCachingServices; used by EnsureExternalResources (taskflow:provision) and new TaskFlowRabbitMqTopologyStartup. OpenTelemetry.Api pin 1.11.2 -> 1.18.0 (old pin NU1902). LogEventIds: InfrastructureMessagingRabbitMqBase = 20000. Pre-existing noise: a second consecutive solution build emits ~180 ExtensionsMetadataGenerator warnings from a stale TaskFlow.Functions obj WorkerExtensions tree; clean-obj build is 0 warnings (G4: record in REFERENCE-STATUS). P5 running; spawning G3.
- 2026-09-08: integration gate on 24dc873 (adds G2): build 51 projects 0 warnings (after cleaning the Functions obj tree), fast lane 595 passed. In flight: P5, G3.
- 2026-09-08: P5 merged (33 files; LogMessages.cs event-id collision with G2 resolved by renumbering P5's two events to BootstrapperBase + 13/14). Shapes: OpenAICompatibleChatClientFactory (AiServices:Endpoint/ApiKey/ChatModel/EmbeddingModel; IChatClient + IEmbeddingGenerator<string, Embedding<float>>); AzureInference arm wires an embedding generator only when ConnectionStrings:embeddings exists; OpenAI and Microsoft.Extensions.AI.OpenAI are deployed dependencies again (no PrivateAssets, R2R exclude removed, publish output contains OpenAI.dll; no NU1605 existed). RegisterServices.AppConfiguration.cs (AppConfig:Endpoint or ConnectionStrings:AppConfig; sentinel TaskFlow:Sentinel; Key Vault refs; feature flags) + AddTaskFlowFeatureManagement inside RegisterInfrastructureServices (Api, Scheduler, Functions); Gateway has its own GatewayAppConfiguration.cs (no FeatureManagement); TenantTargetingContextAccessor; TaskFlowFeatures constants in Application.Contracts (TaskViews, Export, SemanticSearch, AiReview); FeatureGateEndpointFilter/RequireFeature -> 404 applied to TaskViews group and Export; AiTaskReviewer checks AiReview via IVariantFeatureManager; SemanticSearch defined, applied by P7. Packages: Microsoft.Azure.AppConfiguration.AspNetCore 8.6.0, Microsoft.FeatureManagement(.AspNetCore) 4.7.0. openapi-doc regenerated (Export 404). Open: TaskFlow.Functions.csproj still has OpenAI PrivateAssets=all (Functions never runs OpenAICompatible today); App Configuration source not wired into Functions. G3 running; spawning P7.
- 2026-09-08: integration gate on 073dbe1 (adds P5): build 0 warnings; fast lane 595 (Architecture 45, Endpoint 146, Unit 405). In flight: G3, P7.
- 2026-09-08: G3 merged (35 files; trivial comment conflict in Test.Architecture.csproj). Fast lane 620 in G3's tree. Shapes: src/Shared/TaskFlow.Contracts.Grpc (taskflow_read.proto, TaskFlowReadGrpcMapper); Api Grpc/TaskFlowReadGrpcService (RequireAuthorization, per-status RpcException mapping), Kestrel:Endpoints Http 8080 + Grpc 8081 Http2 in Api appsettings, Dockerfile drops ASPNETCORE_URLS and exposes 8081; Aspire 13.5 derives endpoints from Kestrel:Endpoints (injects Kestrel__Endpoints__Http__Url / Kestrel__Endpoints__Grpc__Url; no WithHttpEndpoint); Blazor AddGrpcClient + ClientReadSettings (Clients:UseGrpcReads) reads summary/metadata over gRPC, address knob Grpc__TaskFlowRead__Address (compose: http://<api>:8081), plus services__taskflowapi__Grpc__0 from WithReference; container-app.bicep additionalPortMappings param, Api internal 8081, Blazor Grpc:TaskFlowRead:Address; main.json regenerated. CacheSettings.Serializer = Json | MessagePack (ContractlessStandardResolver + Lz4BlockArray). DEFECT FIXED: JSON cache options used ReferenceHandler.Preserve, which cannot deserialize positional records, so every L2 read of the summary threw and FusionCache treated it as a miss; Preserve removed, CacheSettings.SchemaVersion 1 -> 2 (G4: add a DESIGN-DECISIONS row D-056 for this). Api Dockerfile literal backslash-n in the publish line fixed. Packages: Grpc.AspNetCore 2.83.0, Grpc.Tools 2.83.0, Google.Protobuf 3.36.1, Grpc.Net.ClientFactory 2.83.0, FusionCache NeueccMessagePack 2.7.2, MessagePack 3.1.8 direct. EF.Grpc 1.0.68 inspected and NOT used (ServiceErrorInterceptor maps everything to Internal and leaks ex.Message into trailers; AddGrpcClient2 forces HttpClientHandler; no XML docs) - G4 adds package requests. Open for the user: launchSettings applicationUrl (5188/7067) now conflicts with Kestrel:Endpoints on local dotnet run (one override warning). PRE-EXISTING BUG for P6: infra/main.bicep sets Blazor ApiBaseUrl while Blazor Program.cs reads Gateway:BaseUrl - the Blazor container app would fail startup in Azure. Podman IP: use 1.1.1.1 via 192.168.176.1 dev eth0 src 192.168.186.166 uid 0 
    cache  (hostname -I is unavailable). P7 running; spawning P6.
- 2026-09-08: integration gate on 55b1309 (adds G3): dotnet test: 635 tests passed. In flight: P6, P7.
- 2026-09-08: P6 merged (25 files). Fast lane 643 in P6's tree; Test.Aspire 12 non-DCP tests pass locally (7 new lane tests). Shapes: src/Host/Aspire/AppHost/LaneDefaults.cs (TASKFLOW_LANE > Hosting:Lane > Azure; Azure lane byte-identical, only Hosting__Lane=Azure added); Portable = Postgres + RabbitMQ + minio container (endpoints s3 9000 / console 9001, dev creds as parameters, bucket provisioned by the app startup task), no Azure emulators/Cosmos/Functions/Foundry; helpers WithObjectStorage/WithLaneEnvironment/WithAuditSink beside WithBroker. deploy/compose/: docker-compose.yml, docker-compose.override.local.yml (profile local, build: with the nuget secret), Caddyfile + Caddyfile.local (active health_uri /healthz/ready on gateway and blazor; chiseled app images have no shell so no compose healthcheck), .env.example (names only, test-enforced), images.env.example, pgbouncer/ (edoburu/pgbouncer, transaction mode), README.md runbook; .gitignore covers .env, images.env, userlist.txt. Bicep: postgresPgBouncerEnabled -> pgbouncer.enabled + port 6432 + Database__PostgreSql__PoolerMode=Transaction; Blazor env fixed ApiBaseUrl -> Gateway__BaseUrl (contract test pins it). CI: .github/workflows/build-images.yml reusable (digest outputs) used by deploy.yml and new deploy-vps.yml (dispatch deploy|rollback, plain ssh + VPS_KNOWN_HOSTS, secrets VPS_HOST/VPS_USER/VPS_SSH_KEY/VPS_KNOWN_HOSTS/CADDY_DOMAIN); ci.yml compose config -q every run + dispatch-gated compose-smoke. Pre-existing defects fixed: Gateway Dockerfile literal backslash-n; DeploymentWorkflowContractTests had no TestCategory (never ran) and asserted the plain chiseled base G1 moved - the ci.yml contract gate had been failing since G1. Floating tags (otel-lgtm, minio, pgbouncer) flagged in the runbook. Nothing in the compose/VPS path has executed on this machine; CI compose-smoke is the first real run. P7 reported (647 fast lane; PostgreSql lane 61 passed/1 skipped, SqlServer 59/3 skipped; Pgvector 0.3.2 + Pgvector.EntityFrameworkCore 0.3.0 targets net8.0/Npgsql >= 9.0.1 rolling forward onto 10.0.3); corrections sent: remove the new Infrastructure.AI -> Infrastructure.Data reference (guard moves to Bootstrapper RegisterServices.VectorSearch.cs), artifact search default for Portable = Sql (PgVector opt-in), merge P6 and regenerate main.json.
- 2026-09-08: integration gate on d41e7b6 (adds P6): dotnet test: 643 tests passed, build 0 warnings. Waiting on P7 corrections.
- 2026-09-08: P7 merged (46 files) after corrections: Infrastructure.AI references only Contracts/Models/Observability; PostgreSQL prerequisite guard lives in Bootstrapper Registration/RegisterServices.VectorSearch.cs; artifact hostingLaneDefaults.Portable.searchProvider = Sql (PgVector opt-in). Fast lane 657 in P7's tree (Unit 423, Endpoint 164, Architecture 50, RabbitMq 15, PlaywrightUI 5); PostgreSql lane 61 passed/1 skipped, SqlServer 59/3 skipped; 6 migration pairs clean. Shapes: TaskItemContentChangedEvent raised by TaskItem.Update on title/description change (registered in TaskFlowMessagingJsonContext + Versions); TaskItemEmbedding (vector(1536) constant DefaultDimensions, HNSW cosine, Npgsql-only mapping in OnModelCreating, PostgreSql migration AddTaskItemEmbedding only); ITaskEmbeddingRepository/PgVectorTaskEmbeddingRepository; TaskEmbeddingConsumer (inbox-guarded) registered only when Search resolves to PgVector; conditional topology: RabbitMQ queue taskflow.embedding, Service Bus subscription embedding (searchProvider Bicep param), FunctionEmbeddingTrigger.ProcessTaskEmbedding disabled by name unless its subscription exists; PgVectorSearchService for SearchMode.Semantic, SemanticSearch flag via FeatureGateEndpointFilter narrowing predicate (404 when off); Search:PgVector:Dimensions must equal 1536 else startup throws. Packages: Pgvector 0.3.2, Pgvector.EntityFrameworkCore 0.3.0 (targets net8.0, Npgsql >= 9.0.1, rolls forward onto 10.0.3 - G4 records as a watch item). ALL CODE SLICES MERGED. Next: full acceptance on the merged tree, then G4.
- 2026-09-08: FULL ACCEPTANCE on d4d42ee (all code slices merged): build 52 projects 0 warnings; Uno 0 warnings; fast lane 657; SqlServer lane Integration 59 / E2E 10 / FlowEngine 18; PostgreSql lane Integration 61 / E2E 10 / FlowEngine 18; EF.Messaging.RabbitMq.Tests 31; Test.UI 56; Test.Aspire non-DCP 12; vulnerable packages 0. Unverified here (CI-only): Test.Aspire DCP lanes, PlaywrightUI full-stack, compose-smoke, deploy-vps; Test.Mutation not rerun (last 33). Counts sent to G4 for REFERENCE-STATUS. G4 running (LoggerMessage sweep + docs); after its merge: final fast-lane gate, then closing report. No push, no PR until the user asks.
- 2026-09-08: G4 merged (56 files): 58 raw log calls converted to [LoggerMessage] across 30 files, src/.editorconfig sets CA1848=error for src only; docs/plans/scale-guidance-alignment.md (26 rows verified with file:line); tech-design 9.3 Portable Topology + docs/diagrams/diagram-23.svg (diagram-10 was taken), sections 4, 5.5, 7.2, 8.6, 8.7, 10.1, 10.2, 13.3 patched; D-056 appended; ef-package-requests 25-32 (verified by decompiling the installed EF.* assemblies - none ship XML docs); scaffold-instructions-handoff sections 18-38; README/AGENTS lane lines + LogEventIds table + podman IP command; REFERENCE-STATUS refreshed from observed counts; Azure.AI.OpenAI and Aspire.Hosting.Azure.KeyVault pins removed. ALL SLICES DONE. Open items for the user: push/PR decision; compose/VPS path unexecuted on any dev machine (CI compose-smoke is the first real run); Functions host lacks App Configuration wiring and keeps OpenAI PrivateAssets=all; launchSettings applicationUrl vs Kestrel:Endpoints warning on local dotnet run; Pgvector.EntityFrameworkCore 0.3.0 targets net8.0; floating image tags in compose; TaskItemRescheduledEvent dead code; EF.Grpc IncludeLogDataInResponse leak (package request 29). Next action: final fast-lane gate on the merged tree, then closing report; none after that until the user decides on push/PR.
- 2026-09-08: FINAL GATE on 393ab0d: build 0 warnings (slnx and Uno), fast lane 657 passed, working tree clean; 183 commits over main, 266 files changed since 6c3cc4f in this refactor. Refactor complete. Next action: none until the user decides on push/PR.
- 2026-09-09: user asked for item-4 fixes (latest components, no compat). F1 first pass done (Functions App Configuration via Microsoft.Azure.AppConfiguration.Functions.Worker 8.6.0 + OpenAI deployed in Functions; Api launchSettings applicationUrl and https profile removed - Kestrel:Endpoints is the single source, verified live; Pgvector.EntityFrameworkCore 0.3.0 is the newest release on nuget.org (targets net8.0, rolls forward, PostgreSql lane is the proof) - wording fixed; compose/AppHost tags pinned otel-lgtm 0.32.1, minio RELEASE.2025-09-07T16-13-09Z (community image unmaintained since 2025-09, runbook notes it), pgbouncer v1.25.2-p0; fast lane 658). BUG FOUND by F1's live run: InvariantGlobalization=true (D-047) makes Microsoft.Data.SqlClient throw NotSupportedException on connection open - the Api, Scheduler and DatabaseMigrator containers would fail on the SQL Server lane; never caught because tests do not import TaskFlow.Host.props and the compose lane is Postgres. F1 continues on the same branch: InvariantGlobalization=false for Api/Scheduler/DatabaseMigrator with -chiseled-extra images (ICU), architecture rule that SqlClient hosts are never invariant, live proof against LocalDB, D-047 text update. Blazor sticky sessions: answered as an explicit UI-tier exception (Blazor Server circuits are replica-bound); WebAssembly render mode offered as the stateless alternative, awaiting the user.
